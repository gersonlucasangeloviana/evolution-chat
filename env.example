# Auth básica para proteger a interface (recomendado)
ADMIN_USER=admin
ADMIN_PASSWORD=troque-esta-senha

# Conexão com o Postgres (uma das duas opções abaixo)
# 1) URL única
# DATABASE_URL=postgres://usuario:senha@host:5432/banco

# 2) Campos separados
PGHOST=postgres
PGPORT=5432
PGUSER=usuario
PGPASSWORD=senha
PGDATABASE=evolution
PGSSL=

# Schema (padrão public)
PGSCHEMA=public

# Porta da aplicação
PORT=3000
